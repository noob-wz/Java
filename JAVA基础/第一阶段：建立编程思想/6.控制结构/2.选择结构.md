在 Java 中，选择结构主要分为两大派系：

1. **`if` 系列**：万能逻辑王。适合处理**区间范围**（如：分数 60-100）或**复杂布尔逻辑**。
2. **`switch` 系列**：精准匹配王。适合处理**固定离散值**（如：星期一、星期二、菜单选项）。

---

## ➡️ The `if` Family (条件判断)

这是最常见的选择结构，根据一个**布尔表达式**（判断结果为 **真** 或 **假** 的表达式）来选择执行路径。（在 C 语言 中，条件表达式不必非得是布尔表达式，注意区分）
**A. 单分支 (`if`)**

* **语义**：“如果满足条件，就做某事；否则什么都不做。”

```java
if (score >= 60) {
    System.out.println("及格了");
}

```

**B. 双分支 (`if-else`)**

* **语义**：“如果满足条件，做 A；否则，做 B。”（二选一，必选其一）

```java
if (score >= 60) {
    System.out.println("恭喜及格");
} else {
    System.out.println("下次努力");
}

```

**C. 多分支 (`if-else if-else`)**

* **语义**：“是 A 吗？不是。那是 B 吗？不是。那是 C 吗？...”
* **注意**：一旦满足某个条件，执行完对应代码块后，**直接跳出**整个结构，不会往下看了。

```java
int score = 85;

if (score >= 90) {
    System.out.println("优秀");
} else if (score >= 80) {
    System.out.println("良好"); // 这里满足，输出后直接结束
} else if (score >= 60) {
    System.out.println("及格"); // 即使满足，也不会执行，因为上面已经拦截了
} else {
    System.out.println("不及格");
}

```

> **⚠️ 注意**
>
> - (1) `if` 后的代码块最好用花括号 `{}` 包围，否则**不写大括号的情况下， if 只控制紧随其后的第一条语句** ，后期维护不清晰。
> - (2) 条件控制结构的条件判断是**从第一个条件开始直到某一个条件成立，而不是直接跳转到成立的条件**。换句话说，在执行某个条件成立后的代码前，也许已经执行过了几次条件判断，这就需要 **注意条件判断中的相同方法重复调用，比如`sc.next()`**
> - (3) **编程规范：** else 关键字必须且只能紧接在它所配对的 `if` 语句所控制的单条语句或语句块 `{}` 之后，任何插入到两者之间的独立语句都会破坏这种配对关系，导致编译错误。所以，**为了清晰和避免错误，即使 `if` 或 `else` 块中只有一条语句，也应该使用花括号 `{}` 明确限定其作用域。**

---

## ➡️  The `switch` Family (开关选择)

**版本 1：传统 Switch (Java 12 之前)**

* **痛点**：必须写 `break`，否则会发生**穿透 (Fall-through)** 现象（继续执行下一个 case）。
* **支持类型**：`byte`, `short`, `int`, `char`, `String` (JDK 7+), `Enum`.

```java
int day = 2;
switch (day) {
    case 1:
        System.out.println("周一");
        break; // ⚠️ 必须写，忘了就是 Bug
    case 2:
        System.out.println("周二");
        break;
    default:
        System.out.println("未知");
        break;
}

```

**版本 2：增强版 Switch (Java 12/14+ 新特性)**

* **亮点**：引入了 `->` 箭头语法。
* **优势**：**不需要写 break**，自动终止，不会穿透。还能当“表达式”直接返回值。

```java
int day = 2;
// 这是一个【表达式】，可以直接赋值给变量 result
String result = switch (day) {
    case 1 -> "周一";
    case 2 -> "周二";     // ✅ 简洁，无穿透风险
    case 3, 4, 5 -> "工作日"; // ✅ 支持多值匹配
    default -> "未知";
};
System.out.println(result);

```


### 组成部分详解

#### ① `expression` (控制表达式)

- **返回值类型要求：** 必须是**整型、字符串、枚举类型**，包括 `int`, `char`,`byte`, `short`, `enum` , `String`这六种类型。（没有`long`）
- **作用：** 它的值决定了程序将跳转到哪个 `case` 标签。

#### ② `case` 标签

- **标签要求：** 必须是**常量表达式**，即在编译时就能确定值的常量，**不能是变量**。**注意：** `123`是常量表达式，**`123 + 1`也是常量表达式**
- **统一或兼容：** `case`值必须和控制表达式的返回值 **统一** 或者 **兼容**（比如 byte、char、int 等之间）。
  - 如果`expression` 的值与某个 `case` 后面的常量值是同一数据类型，不会发生数据类型转换，直接比较
  - 如果`expression` 的值与某个 `case` 后面的常量值**不是同一数据类型，会先进行数据类型提升**，再进行比较
- **唯一性：** 同一个 `switch` 语句内不能有重复的 `case` 值。
  - **当表达式返回值是基础数据类型时：判断 控制表达式的值和 case 的值是否相等 等价于 `if(a == b)`**。当 case 的值是兼容的整型时，**本质比较的是它们在内存中的整数值**，此时'A', 65 其实都是一样的
  - **当表达式返回值是`String`类型：判断 控制表达式的值和 case 的值是否相等 等价于 使用.equals()方法，而不是`==`**
- **执行起点：** `expression` 的值会从第一个`case`开始比较，当与某个 `case` 后面的常量值匹配，程序将**从此 `case` 标签后的第一条语句开始执行**。

#### ③ `break` 语句（关键）

- **作用：** `break` 语句用于**立即跳出**当前的 `switch` 语句块，将控制权转移到 `switch` 块之后的语句。
- **重要性：** 如果缺少 `break`，程序将继续向下执行下一个 `case` 标签中的代码，直到遇到 `break` 或 `switch` 语句结束。这被称为 **"穿透"**。
- **编程规范：** 即使是**最后一个 `case` 后都要加一个`break`**，防止在以后添加新的 case 或者 default 时导致穿透

#### ④ `default` 标签

- **作用：** 当 `expression` 的值**不匹配任何** `case` 标签时，控制权会转移到 `default` 标签后的语句。
- **位置：** 它可以放在 `switch` 块内的任何位置，但习惯上放在最后。
- **编程规范：** 虽然`default`是可选的，但**建议 `switch` 的结尾都用`default` 结束**，来处理所有未预料到的输入

---

## `switch` 核心机制：穿透 ⚠️

**这是 `switch` 语句与 `if-else if` 最大的区别，也是新手最容易出错的地方。**

- 如果一个 `case` 分支**没有 `break`** 语句，程序执行完该分支的代码后，会**继续执行的所有分支代码**，而**不再检查**之后 `case` 的条件是否匹配。

**🌰 穿透示例：**

```java
int score = 2;

switch (score) {
    case 1:
        printf("得分 1\n");
    case 2:
        printf("得分 2\n"); // 从这里开始执行
    case 3:
        printf("得分 3\n"); // 穿透，也会执行
        break;             // 遇到 break，跳出 switch
    default:
        printf("未得分\n");
}
// 输出:
// 得分 2
// 得分 3
```

- **正面应用：** 穿透有时被用来简化代码，例如当多个 `case` 标签执行相同的操作时，可以将它们堆叠起来，只在最后一个 `case` 处放置 `break`。

  ```java
  char grade = 'B';
  switch (grade) {
    case 'A':
        printf("优秀!\n");
        break;
    case 'B':
    case 'C': // 穿透：B 或 C 都执行下面的代码
        printf("良好或合格\n");
        break;
    case 'D':
        printf("需努力\n");
        break;
    default:
        printf("成绩无效\n");
  }
  // 结果: 输出 "良好或合格"
  ```

---

### 💼 大厂实战模式：卫语句 (Guard Clauses)

这是从新手进阶到资深开发者的**关键技巧**。
为了避免代码出现“箭头型”的深层嵌套（Nested Ifs），大厂代码规范推荐使用**卫语句**。

**❌ 丑陋的嵌套 (Arrow Code)**

```java
void process(String username, String password) {
    if (username != null) {
        if (password != null) {
            if (password.length() > 6) {
                // 真正的业务逻辑在这里...
                login();
            }
        }
    }
}

```

**✅ 优雅的卫语句 (Fail Fast)**
先把所有**异常情况**筛出来返回，最后剩下的就是正常逻辑。

```java
void process(String username, String password) {
    // 卫语句 1: 用户名为空，直接滚蛋
    if (username == null) return; 
    
    // 卫语句 2: 密码为空，直接滚蛋
    if (password == null) return;
    
    // 卫语句 3: 长度不够，直接滚蛋
    if (password.length() <= 6) return;

    // --- 核心逻辑畅通无阻 ---
    login();
}

```

---

### ⚠️ 常见踩坑与规范

#### 1. 悬挂 Else 问题 (Dangling Else)

Java 编译器总是把 `else` 和**最近的**那个 `if` 配对。

```java
int x = 10, y = 20;

if (x > 5)
    if (y > 30) 
        System.out.println("A");
else // ⚠️ 视觉上以为是对应第一个 if，其实它对应的是 y > 30 这个 if
    System.out.println("B");

// 输出结果：B (因为 y 不大于 30)

```

**💡 规范**：**永远永远要加花括号 `{}**`，即使代码只有一行！

```java
if (x > 5) {
    if (y > 30) {
        System.out.println("A");
    } else {
        System.out.println("B");
    }
}

```

#### 2. Switch 的穿透 (Fall-through)

除非你故意想利用穿透（比如几个 Case 执行同一逻辑），否则在老式写法中漏掉 `break` 是最常见的 Bug。

#### 3. 浮点数的相等判断

**严禁**在 `if` 或 `switch` 中使用 `double` 进行精确相等 (`==`) 判断。

* ❌ `if (0.1 + 0.2 == 0.3)` -> 结果是 false。
* ✅ 应该判断差值是否小于极小值 (Epsilon)。

#### 4. 条件里写副作用
```java
if (i++ > 5) { ... } // ❌ 可读性极差，极易出 bug
```
* **大厂规范**：为了避免逻辑混乱，**条件表达式里不产生副作用**

---

### 📊 决策树：我该选哪种结构？

| 场景特点 | 推荐结构 | 理由 |
| --- | --- | --- |
| **判断范围** (如 `x > 50 && x < 100`) | **if-else** | switch 只能处理离散值，处理不了区间 |
| **固定选项少** (如 2-3 个) | **if-else** | 写起来比 switch 快 |
| **固定选项多** (如 5 个以上) | **switch** | 结构清晰，编译器有跳转表优化，性能略好 |
| **复杂的组合逻辑** (`A && B |  | !C`) |
| **需要返回值** (Java 14+) | **Switch 表达式** | `var x = switch(...)` 极其优雅 |

---
