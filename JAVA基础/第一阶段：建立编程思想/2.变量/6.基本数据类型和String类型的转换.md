Java 中的基本数据类型（`int`, `double`, `char` 等）与 `String`（引用类型）之间的转换，是编程中最常见的操作之一。由于 `String` 不是数值类型，这些转换都需要依赖特定的方法。

转换主要分为两个方向：

---

## 1\. 基本数据类型 $\rightarrow$ `String`（自动且安全）

这个方向的转换是安全的，因为所有基本类型都可以无损地表示为文本。有**三种**方法：

### 方法一：字符串连接符（最常用）

将任何基本类型与一个空字符串 `""` 或任何其他字符串通过 `+` 连接符相连，Java 编译器会自动将其提升为 `String`。

```java
int i = 100;
String s1 = i + ""; // 结果: "100" (String)

double d = 3.14;
String s2 = d + " is pi"; // 结果: "3.14 is pi" (String)
```

### 方法二：`String.valueOf()` 方法（推荐）

这是最标准和推荐的方法，它能处理所有基本类型，并将它们转化为其字符串表示形式。

```java
int i = 100;
String s1 = String.valueOf(i); // 结果: "100"

boolean b = true;
String s2 = String.valueOf(b); // 结果: "true"
```

### 方法三：包装类 `toString()` 方法

使用对应包装类的静态 `toString()` 方法。

```java
int i = 100;
String s = Integer.toString(i); // 结果: "100"
```

---

## 2\. `String` $\rightarrow$ 基本数据类型（需要解析，存在风险）

这个方向的转换是危险的，因为 `String` **必须包含可以被解析为数值的有效字符**。如果字符串格式不正确，程序将抛出异常。

### 核心方法：包装类的 `parse` 方法

所有数值型的基本数据类型都必须通过其对应 **包装类（Wrapper Class）** 的静态 `parse` 方法进行转换。

| 基本类型  | 包装类    | 转换方法                         | 风险（异常）                         |
| :-------- | :-------- | :------------------------------- | :----------------------------------- |
| `int`     | `Integer` | `Integer.parseInt(String s)`     | `NumberFormatException`              |
| `double`  | `Double`  | `Double.parseDouble(String s)`   | `NumberFormatException`              |
| `long`    | `Long`    | `Long.parseLong(String s)`       | `NumberFormatException`              |
| `byte`    | `Byte`    | `Byte.parseByte(String s)`       | `NumberFormatException`              |
| `boolean` | `Boolean` | `Boolean.parseBoolean(String s)` | 无异常，非 `true` 字符串返回 `false` |

#### 示例：

```java
String strInt = "123";
int i = Integer.parseInt(strInt); // i = 123

String strDouble = "9.8";
double d = Double.parseDouble(strDouble); // d = 9.8

String strBool = "TRUE";
boolean b = Boolean.parseBoolean(strBool); // b = true (不区分大小写)
```

### 🚨 风险提示：`NumberFormatException`

当尝试将一个格式错误的 `String` 转换为数值类型时，JVM 将抛出 `NumberFormatException`：

```java
String errorStr = "123A";
int i = Integer.parseInt(errorStr); // 运行时抛出 NumberFormatException
// 错误原因：字符串中包含非数字字符 'A'
```

在实际编程中，所有 `String` 到数值的转换都必须使用 **`try-catch` 块**来处理这种潜在的异常。
