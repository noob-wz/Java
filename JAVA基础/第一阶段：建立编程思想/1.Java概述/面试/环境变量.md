**任务：** 如果我删除了 `JAVA_HOME` 变量，但在 `Path` 变量中直接写死了 `C:\Program Files\Java\jdk-17\bin。`**

1. **`javac` 命令还能在命令行里运行吗？**
2. **这样做会有什么长期的潜在隐患？**

**答：**
1. 可以运行。因为操作系统只要在 Path 里找到了 bin 目录，就能找到 javac。
2. 隐患：后续安装大厂开发工具（如 Maven, Tomcat）时，它们会报错说找不到 JAVA_HOME；且升级 JDK 版本时，需要手动修改多处 Path，非常麻烦。

