“既然 Java 是先编译成字节码再执行，那为什么我们常说 Java 是‘解释型’语言？它真的比 C++ 慢吗？”

底层逻辑：  
1.解释执行： JVM 里的 Interpreter 会一行行读字节码，翻译成机器码执行（这确实慢）
2.JIT (Just-In-Time) 编译器： 重点来了！当 JVM 发现某段代码运行得非常频繁（热点代码），它会瞬间启动 JIT，直接把这段字节码编译成本地机器码并缓存起来。  
3.结果： 运行一段时间后，Java 的速度在某些场景下甚至能追平 C++。
