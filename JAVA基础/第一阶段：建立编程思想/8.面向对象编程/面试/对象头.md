## 问题 1：“一个空对象 Object obj = new Object(); 真的不占空间吗？它在内存里到底占多大？”

**底层逻辑：**

1. **Mark Word（8 字节）：** 存储哈希码、GC 年龄、锁状态。
2. **Klass Pointer（4 或 8 字节）：** 指向类元数据的指针。
3. **Padding（填充）：** JVM 要求对象占用的字节数必须是 **8 的倍数**（为了 CPU 读取效率）。

**结论：** 哪怕是一个空对象，也会占用 **16 字节** 左右的空间。这在处理“海量小对象”的架构方案时，是必须要计算的内存成本。
